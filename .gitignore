__pycache__/
*.py[cod]

# 运行时生成的数据（每个部署各自生成，不入库；* 覆盖 .tmp/.corrupt 侧车）
data_learning.json*
data_monitor.db*
data_monitor.json.imported
*.log

# 本地私有规则与备注（永不入库）
AGENTS.local.md

# 本机容器运行残骸（测试容器以插件目录为 cwd 时生成的 data/ 子目录）
/data/
